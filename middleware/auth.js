//PALCEHOLDER FILE

const auth = function (req, res, next) {
    console.log("insinde auth");

    //if not user page or user auth
    //if auth continue
    //if not redirect
    next();
};

module.exports = auth;